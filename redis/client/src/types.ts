export type User = {
  uuid: string;
  firstName: string;
  lastName: string;
  score: number;
  date: string;
}

export type Score = {
  userId: string;
  firstName: string;
  lastName: string;
  score: number;
  date: string;
}